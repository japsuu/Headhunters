using Mirror;
using UnityEngine;
/*
public interface ICarriable
{
    /// <summary>
    /// Returns the object that will be moved to a carryPosition.
    /// Could be an object instantiated by the carryable, or the original object.
    /// </summary>
    /// <returns></returns>
    public GameObject GetCarriableObject();

    /// <summary>
    /// Returns the object that will be spawned once the carryable is dropped.
    /// Could be an object instantiated by the carryable, or the original object.
    /// </summary>
    /// <returns></returns>
    public GameObject GetSpawnableObject();

    /// <summary>
    /// Called AFTER the item has been parented.
    /// </summary>
    public void OnPickedUp();

    /// <summary>
    /// Called AFTER the carryable has been unparented.
    /// </summary>
    public void OnDropped();
}
*/